import {MapboxRender, Settings, RenderOptions} from "./render";
import { Projection, Tile} from "./projection";




export interface GeneratorOptions {
    mapboxRenderOptions: Settings
}

export class TileGenerator{

}